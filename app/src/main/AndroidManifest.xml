<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.saransklife" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
		android:name=".App_"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".SplashActivity_"
            android:label="@string/title_activity_splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

		<activity
			android:name=".MainActivity_"
			android:launchMode="singleTop"/>

		<activity
			android:name=".page.PagesActivity_"
			android:launchMode="singleTop"/>

		<activity
			android:name=".place.PlaceCategoriesActivity_"
			android:launchMode="singleTop"/>

		<activity
			android:name=".place.EntitiesByCategoryActivity_"
			android:parentActivityName=".place.PlaceCategoriesActivity_"
			android:launchMode="singleTop"/>

		<activity
			android:name=".place.EntityActivity_"
			android:parentActivityName=".place.EntitiesByCategoryActivity_"/>

		<activity
			android:name=".event.EventsActivity_" />

		<activity
			android:name=".event.EventInfoActivity_" />

	</application>

</manifest>
